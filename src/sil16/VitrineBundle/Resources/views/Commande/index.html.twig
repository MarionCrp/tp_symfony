{% extends "sil16VitrineBundle::layout.html.twig" %}

{% block title %} Vos commandes {% endblock %}
{% block h1 %} Vos commandes {% endblock %}

{% block content %}

  <div id="dg">
    <div class="container">
      <div class="row centered">
        <br>
        {% if commandes|length > 0 %}
          {% for commande in commandes %}
            {% include 'sil16VitrineBundle:Commande:_order_details.html.twig' with {'commande': commande}  %}
          {% endfor %}
        {% else %}
           <h2> Aucune commande effectuée </h2>
        {% endif %}
      </div><!-- row -->
    </div><!-- container -->
  </div><!-- DG -->
{% endblock %}
