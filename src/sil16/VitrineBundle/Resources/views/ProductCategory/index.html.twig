{% extends "sil16VitrineBundle::layout.html.twig" %}

{% block title %} Catalogue {% endblock %}
{% block h1 %} Catalogue {% endblock %}

{% block content %}

	<!-- CATALOGUE SECTION -->
	<div id="dg">
		<div class="container">
      <div class="row centered">
        <br>
      {% for result in results %}

  				<div class="col-lg-3">
  					<div class="tilt">
              <a href="{{ path('sil16_vitrine_articles_par_categorie', { product_category_id: result['product_category'].getId() } )  }}">
                <h2> {{ result['product_category'].name() }} </h2>
                <div class="price">{{ result['active_products_count'] }} article{% if(result['active_products_count'] > 1) %}s {% endif %} </div>
              </a>
            </div>
  				</div>
        {% endfor %}

			</div><!-- row -->
		</div><!-- container -->
	</div><!-- DG -->
{% endblock %}
